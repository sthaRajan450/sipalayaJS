const numbers = [8, 3, 7, 5];
let count = 0;
for (let i = 0; i < numbers.length; i++) {
  count++;
}
console.log(`Number of elements in the array is ${count}`);

// or

console.log(`Number of elements in the array is ${numbers.length}`);
