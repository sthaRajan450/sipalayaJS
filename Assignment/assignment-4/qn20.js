const array = ["apple", "banana", "cherry"];
const itemLength = [];
for (let i = 0; i < array.length; i++) {
  itemLength.push(array[i].length);
}
console.log(itemLength);

// const itemLength = array.map(item => item.length);

// console.log(itemLength);]
